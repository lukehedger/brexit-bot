import React from 'react';
// import styles from 'css/components/message.css';

const Component = ({ children }) => {

  // TODO - just renders children, like text, options and input

  return (
    <div>
      {children}
    </div>
  );

}

export default Component;
